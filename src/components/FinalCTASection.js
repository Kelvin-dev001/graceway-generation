export default function FinalCTASection() {
  return (
    <section className="px-6 py-20 bg-primary text-white text-center">
      <div className="max-w-3xl mx-auto space-y-6">
        <h2 className="text-3xl md:text-4xl font-bold">
          This Generation Belongs to God.
        </h2>
        <button className="inline-flex items-center justify-center rounded-full bg-white text-primary px-6 py-3 text-sm font-semibold shadow-soft transition hover:opacity-90">
          Reserve My Spot
        </button>
      </div>
    </section>
  );
}